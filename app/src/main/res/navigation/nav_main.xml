<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main"
    app:startDestination="@+id/ic_home">
    <fragment
        android:id="@+id/ic_home"
        android:name="com.cursoandroid.instagram.fragment.FeedFragment"
        android:label="fragment_feed"
        tools:layout="@layout/fragment_feed" />
    <fragment
        android:id="@+id/ic_pesquisa"
        android:name="com.cursoandroid.instagram.fragment.PesquisaFragment"
        android:label="fragment_pesquisa"
        tools:layout="@layout/fragment_pesquisa" />
    <fragment
        android:id="@+id/ic_perfil"
        android:name="com.cursoandroid.instagram.fragment.PerfilFragment"
        android:label="fragment_perfil"
        tools:layout="@layout/fragment_perfil" />
    <fragment
        android:id="@+id/ic_postagem"
        android:name="com.cursoandroid.instagram.fragment.PostagemFragment"
        android:label="fragment_postagem"
        tools:layout="@layout/fragment_postagem" />
</navigation>